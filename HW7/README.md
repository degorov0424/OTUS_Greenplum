# HW7

#### 1. Создал таблицы DV скрипт `dv_tds_tables.sql`

---

#### 2. Запросы изначально взял из HW3, но немного поменял их. Скрипт `queries_tds2.sql`

---

#### 3. Написал аналогичные запросы для DV. Скрипт `queries_dv_tds2.sql`

---

#### 4. Итоги

1) Запрос на DV выполнялся дольше, т.к соединялось больше таблиц.

2) Запрос на DV выполнялся дольше. Помимо того, что соединялось больше таблиц, в данном запросе еще и возник Redistribute Motion, т.к одна из трёх таблиц, не совпадает по ключу распределения с остальными.

3) Аналогично предыдущему

4) Запрос на DV выполнялся дольше, т.к соединялось больше таблиц.

5) Запрос на DV выполнялся дольше, т.к соединялось больше таблиц и возник Redistribute Motion. 

В результате, запросы, выполняемые в DV, ожидаемо работали медленнее из-за увеличения кол-ва JOIN и Motion операций. 
При работе с DV с большим объемом данных, нужно уделять больше времени оптимизации, и в процессе расчета чаще перераспределять данные по новым ключам дистрибуции, например с помощью temp таблиц. 

Для себя делаю вывод, что Data Vault, является более гибкой и масштабируемой методологией, но проигрывает в скорости классическим DWH 

---